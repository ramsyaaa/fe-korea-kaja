export interface AllFaqsPayload {
  per_page: number;
  is_active: boolean;
}

export interface EventSchedulesPayload {
  per_page: number;
  is_active: boolean;
}

export interface AllFinalistPayload {
  per_page: number;
  is_active: boolean;
}
export interface AllMerchandisePackagePayload {
  per_page: number;
  is_active: boolean;
}
export interface AllSpecialRewardPayload {
  per_page: number;
  is_active: boolean;
}
export interface AllPreviousWinnerPayload {
  per_page: number;
  is_active: boolean;
}
