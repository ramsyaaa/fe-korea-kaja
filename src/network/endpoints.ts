import { ENDPOINT_SOURCE } from "src/types/network/container";

const endpoints: ENDPOINT_SOURCE = {
  getExampleRequest: {
    source: "api",
    endpoint: "/paths",
  },
};

export default endpoints;
